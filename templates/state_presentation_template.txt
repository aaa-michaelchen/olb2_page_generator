import { ViewPresentation } from '@clublabs/node-flow';
import { ${page_name} as basePresentation } from '../../Base/presentations';

export const ${page_name}: ViewPresentation = { ...basePresentation };
